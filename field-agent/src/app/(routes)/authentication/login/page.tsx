import { LoginCard } from "@/app/_components/login-card/login-card";

export default function Page() {
	return (
		<section className="grid place-items-center h-screen">
			<LoginCard />
		</section>
	)
}
