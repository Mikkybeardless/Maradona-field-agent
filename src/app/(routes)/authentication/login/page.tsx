import { LoginCard } from "@/app/_components/auth-cards/login-card";

export default function Page() {
  return (
    <section className=" flex justify-center">
      <LoginCard />
    </section>
  );
}
